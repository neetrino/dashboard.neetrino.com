# Инструкция по созданию модулей для Neetrino

## ⚠️ ОБЯЗАТЕЛЬНО К ПРОЧТЕНИЮ ПЕРЕД СОЗДАНИЕМ МОДУЛЯ

**ВНИМАНИЕ!** Перед созданием любого модуля обязательно изучите эту документацию полностью. Несоблюдение правил может привести к конфликтам, ошибкам и неправильной работе системы.

### 🔥 Ключевые моменты:
- Модули отключены по умолчанию
- Статус модуля контролируется только с дашборда
- Используются Font Awesome иконки из CDN
- Обязательная проверка активности модуля

---

## 🎨 Си| Reset | `reset` | 🔴 `#dc2626` | `fa-solid fa-rotate-left` | Сброс сайта |
| Remote Control | `remote-control` | 🟤 `#8b4513` | `fa-solid fa-satellite-dish` | Удаленное управление |
| Delivery | `delivery` | 🔵 `#1abc9c` | `fa-solid fa-truck` | Расчет доставки |
| Chat | `chat` | 🟢 `#2ecc71` | `fa-solid fa-comments` | Плавающие кнопки связи |
| App Manager | `app-manager` | 🔵 `#17a2b8` | `fa-solid fa-mobile-screen-button` | Управление мобильными приложениями |

### 🎯 Свободные цвета для новых модулей:
- 🟣 **Светло-фиолетовый** `#9c88ff` - для интеграций
- 🟠 **Оранжевый** `#f39c12` - для утилитнок - Font Awesome

### ✨ Новая система иконок v2.0.0

Все модули теперь используют **Font Awesome иконки** вместо WordPress Dashicons для более современного и профессионального вида.

#### 📋 Конфигурация модуля в `includes/Module_Config.php`:

```php
'module-name' => [
    'icon' => 'fa-solid fa-icon-name',     // Font Awesome иконка
    'icon_type' => 'font-awesome',        // Тип иконки (новое поле)
    'color' => '#hexcolor',               // Основной цвет
    'color_light' => '#hexcolor',         // Светлый оттенок для hover
    'description' => __('Description', 'neetrino'),
    'css_class' => 'neetrino-module-module-name'
]
```

#### 🌐 Источник иконок:
- **Font Awesome Icons**: https://fontawesome.com/icons
- **Версия**: 6.4.0 (подключается через CDN)
- **Поддерживаемые типы**: 
  - `fa-solid` - сплошные иконки
  - `fa-brands` - бренды (Telegram, Facebook и т.д.)
  - `fa-regular` - контурные иконки

#### ✅ Преимущества новой системы:
- 🎯 Векторные иконки идеального качества
- 🎨 Автоматическое окрашивание в цвета модуля  
- ✨ Hover эффекты и плавные переходы
- 🔄 Единая система для всех модулей
- 🚀 Загрузка через быстрый CDN

#### 🔧 Техническая реализация:
```php
// Автоматическое получение HTML иконки
$icon_html = Neetrino_Module_Config::get_module_icon_html($slug);

// Поддержка обратной совместимости с Dashicons
if ($icon_type === 'font-awesome') {
    return sprintf('<i class="%s"></i>', esc_attr($config['icon']));
} else {
    return sprintf('<span class="dashicons %s"></span>', esc_attr($config['icon']));
}
```| WordPress Design | `wordpress-design` | 🔵 `#6c5ce7` | `dashicons-admin-appearance` | Современный минималистичный дизайн админ-панели |
| Reset | `reset` | 🔴 `#dc2626` | `dashicons-update-alt` | Мгновенный сброс сайта за несколько секунд |

### 🎯 Свободные цвета для новых модулей:
- 🟡 **Желтый** `#f1c40f` - для предупреждений, уведомлений  
- 🟤 **Коричневый** `#8b4513` - для файловых систем, архивов
- 🔵 **Темно-голубой** `#1abc9c` - для аналитики, статистики
- 🟣 **Светло-фиолетовый** `#9c88ff` - для интеграций
- 🟢 **Светло-зеленый** `#2ecc71` - для оптимизации
- 🟠 **Оранжевый** `#d35400` - освободился после Resetе правила именования файлов и классов
- Обязательная проверка активности модуля

---

## Структура модуля

Каждый модуль должен иметь следующую структуру:

```
wp-content/plugins/Neetrino/modules/
└── my-module/
    ├── my-module.php      (основной файл - ОБЯЗАТЕЛЬНО)
    ├── assets/            (CSS, JS файлы - опционально)
    ├── templates/         (шаблоны - опционально)
    └── includes/          (дополнительные файлы - опционально)
```

## Шаги создания нового модуля

### 1. Создание директории
Создайте папку для модуля в `wp-content/plugins/Neetrino/modules/`:
```
wp-content/plugins/Neetrino/modules/my-module/
```

### 2. Создание основного файла
Создайте файл `my-module.php` (имя файла ДОЛЖНО совпадать с именем папки):

```php
<?php
/**
 * Module: My Module
 * Description: Описание вашего модуля
 * Version: 1.0.0
 */

if (!defined('ABSPATH')) {
    exit;
}

class Neetrino_My_Module {
    
    public function __construct() {
        // Инициализация модуля только если он активен
        if (!Neetrino::is_module_active('my-module')) {
            return;
        }
        
        // Хуки и действия модуля
        add_action('init', [$this, 'init']);
        add_action('wp_enqueue_scripts', [$this, 'enqueue_scripts']);
    }
    
    public function init() {
        // Основная логика модуля
    }
    
    public function enqueue_scripts() {
        // Подключение CSS/JS файлов
    }
    
    /**
     * Статический метод для админ-страницы
     * Вызывается автоматически если модуль активен
     */
    public static function admin_page() {
        ?>
        <div class="wrap neetrino-dashboard">
            <div class="neetrino-header">
                <div class="neetrino-header-left">
                    <h1>My Module Settings</h1>
                </div>
            </div>
            <div class="neetrino-content">
                <div class="neetrino-card">
                    <h2>Настройки модуля</h2>
                    <p>Здесь будет интерфейс настроек вашего модуля.</p>
                </div>
            </div>
        </div>
        <?php
    }
}

// Инициализация модуля
new Neetrino_My_Module();
```

### 3. Соглашения об именовании

- **Папка модуля**: `my-module` (lowercase, дефисы)
- **Основной файл**: `my-module.php` (точно как папка)
- **Класс модуля**: `Neetrino_My_Module` (PascalCase с префиксом Neetrino_)
- **Slug модуля**: `my-module` (автоматически из имени папки)

## 🎨 Дизайн модулей на дашборде

### Цветовая схема и иконки

Каждый модуль на дашборде имеет уникальную цветовую схему, состоящую из:

#### ✅ ОБЯЗАТЕЛЬНЫЕ элементы дизайна:
1. **🔢 Уникальный номер модуля** - статичный номер, отображаемый в левом верхнем углу карточки
2. **🎯 Иконка модуля** - используйте подходящую иконку из WordPress Dashicons
3. **🌈 Цветовая линия сверху** - должна совпадать с цветом иконки
4. **🎨 Фон иконки** - полупрозрачная версия основного цвета модуля
5. **🎪 Основной цвет** - уникальный цвет для модуля (избегайте дублирования)

#### 📋 Правила выбора цвета и иконки:

**Цветовая палитра (выберите уникальный цвет):**
- 🟠 Оранжевый (`#ff6600`) - используется Bitrix24
- 🟢 Зеленый (`#46b450`) - используется Maintenance Mode  
- 🔵 Синий (`#3498db`) - используется Login Page
- 🔵 Темно-синий (`#2563eb`) - используется Redirect 301
- 🔵 Голубой (`#0088cc`) - используется Telegram
- 🟣 Фиолетовый (`#9b59b6`) - используется Auto Translate
- 🔴 Красный (`#e74c3c`) - свободен
- 🟡 Желтый (`#f1c40f`) - свободен
- 🟤 Коричневый (`#8b4513`) - свободен

**Иконки (WordPress Dashicons):**
- `dashicons-networking` - сети, интеграции
- `dashicons-admin-tools` - инструменты, техобслуживание
- `dashicons-lock` - безопасность, авторизация
- `dashicons-randomize` - перенаправления, обмен
- `dashicons-format-chat` - общение, чаты
- `dashicons-translation` - переводы, языки
- `dashicons-admin-generic` - универсальные модули
- И другие: https://developer.wordpress.org/resource/dashicons/

#### 💻 Современная техническая реализация:

**1. Добавьте модуль в `includes/Module_Config.php`:**

```php
'your-module' => [
    'icon' => 'fa-solid fa-your-icon',
    'icon_type' => 'font-awesome',
    'color' => '#your-color',
    'color_light' => '#your-light-color',
    'description' => __('Your module description', 'neetrino'),
    'css_class' => 'neetrino-module-your-module'
]
```

**2. CSS стили добавляются автоматически:**
- Цветовые переменные генерируются автоматически
- Font Awesome иконки окрашиваются через CSS переменные  
- Hover эффекты применяются ко всем модулям единообразно

**3. Иконка отображается автоматически:**
```php
// В Dashboard.php используется универсальный метод
echo Neetrino_Module_Config::get_module_icon_html($slug);
```
    $description = __('Description of your module.', 'neetrino');
```

#### 🎨 Пример реализации:

Для модуля с зеленым цветом и иконкой инструментов:

```css
.neetrino-module-my-tool {
    --module-color: #27ae60;
    --module-color-light: #2ecc71;
    border-top-color: #27ae60;
}

.neetrino-module-my-tool .neetrino-module-icon {
    background: linear-gradient(135deg, rgba(39, 174, 96, 0.1) 0%, rgba(46, 204, 113, 0.1) 100%);
}

.neetrino-module-my-tool .neetrino-module-icon .dashicons {
    color: #27ae60;
}

.neetrino-module-my-tool:hover {
    background: linear-gradient(135deg, #fff 0%, #f0fff4 100%);
}
```

```php
} elseif ($slug === 'my-tool') {
    $icon = 'dashicons-admin-tools';
    $color = '#27ae60';
    $description = __('My awesome tool for WordPress.', 'neetrino');
```

### ⚠️ ВАЖНО при добавлении нового модуля:

1. **Выберите уникальный цвет** - не дублируйте существующие
2. **Подберите подходящую иконку** - она должна отражать функционал модуля  
3. **Присвойте уникальный номер** - добавьте модуль в функцию `get_module_number()` в `neetrino.php`
4. **Добавьте CSS стили** в `includes/Admin.php`
5. **Обновите PHP код** определения модулей
6. **Протестируйте** внешний вид на дашборде

## Важные правила

### ✅ ОБЯЗАТЕЛЬНО:
1. **Имя файла = имя папки** (например: `my-module/my-module.php`)
2. **Проверка ABSPATH** в начале файла
3. **Проверка активности модуля** в конструкторе
4. **Класс с префиксом Neetrino_**
5. **🔥 НОВЫЕ МОДУЛИ ОТКЛЮЧЕНЫ ПО УМОЛЧАНИЮ** - не добавляйте в `get_default_active_modules()`
6. **🎨 УНИКАЛЬНАЯ ЦВЕТОВАЯ СХЕМА** - каждый модуль должен иметь свой цвет и иконку на дашборде
7. **📝 КРАТКОЕ ОПИСАНИЕ** - не более 50 символов (пример: "Удаление всех товаров WooCommerce одной кнопкой.")
8. **🔢 СТАТИЧНЫЙ НОМЕР МОДУЛЯ** - при создании нового модуля необходимо присвоить ему уникальный номер

### 🔢 Присвоение номеров модулям

При создании нового модуля **ОБЯЗАТЕЛЬНО** добавьте его в функцию `get_module_number()` в файле `neetrino.php`:

**Текущие занятые номера:**
- `login-page` = **1**
- `maintenance-mode` = **2** 
- `bitrix24` = **3**
- `remote-control` = **4**
- `delivery` = **5**
- `auto-translate` = **6**
- `telegram` = **7**
- `delete` = **8**
- `menu-hierarchy` = **9**
- `redirect-301` = **10**
- `user-switching` = **11**
- `wordpress-design` = **12**
- `reset` = **13**
- `chat` = **14**
- `app-manager` = **15**

**Следующие доступные номера: 16, 17, 18, 19, 20...**

#### Как добавить номер для нового модуля:

1. **Выберите следующий свободный номер** (например, 14)
2. **Откройте файл** `wp-content/plugins/Neetrino/neetrino.php`
3. **Найдите функцию** `get_module_number($module_slug)`
4. **Добавьте свой модуль** в массив `$module_numbers`:

```php
$module_numbers = [
    'login-page' => 1,
    'maintenance-mode' => 2,
    // ...существующие модули...
    'reset' => 13,
    'my-new-module' => 14,  // ← Добавьте ваш модуль здесь
];
```

#### ⚠️ ВАЖНО:
- **НЕ ДУБЛИРУЙТЕ НОМЕРА** - каждый модуль должен иметь уникальный номер
- **НЕ МЕНЯЙТЕ** номера существующих модулей
- **ВЫБИРАЙТЕ НОМЕРА ПОСЛЕДОВАТЕЛЬНО** - используйте следующий доступный номер
- **НОМЕРА СТАТИЧНЫ** - они не меняются при включении/отключении модулей

### ✅ РЕКОМЕНДУЕТСЯ:
1. **Метод admin_page()** для страницы настроек в админке
2. **Использование neetrino-* CSS классов** для единого стиля
3. **Namespace или префикс** для избежания конфликтов
4. **Комментарии на русском языке** для консистентности
5. **🎯 Подходящая иконка Dashicons** отражающая функционал модуля

### 📝 Правила для описаний модулей:
**Максимум 50 символов** - используйте краткие и понятные формулировки:

✅ **Правильно:**
- "Удаление товаров WooCommerce одной кнопкой."
- "Быстрое переключение между пользователями."
- "Настройка иерархии меню WooCommerce."
- "Интеграция с CRM системой Bitrix24."
- "Автоматическое определение языка и перенаправление."

❌ **Неправильно (слишком длинно):**
- "Быстрое переключение между учетными записями пользователей для администраторов."
- "Автоматическая настройка иерархии меню на основе структуры категорий WooCommerce."
- "Automatic language detection and redirection based on user's country."

### ❌ НЕ ДЕЛАЙТЕ:
1. Не используйте глобальные переменные без префикса
2. Не создавайте модули без проверки активности
3. Не нарушайте соглашения об именовании
4. **НЕ ДОБАВЛЯЙТЕ новые модули в список активных по умолчанию**
5. **НЕ СОЗДАВАЙТЕ собственные переключатели включения/отключения модуля**
6. **НЕ ДУБЛИРУЙТЕ функции управления модулем** - используйте только дашборд
7. **🚫 НЕ ДУБЛИРУЙТЕ ЦВЕТА** других модулей - выберите уникальный цвет

## Автоматическое обнаружение

Система автоматически обнаружит ваш модуль если:
- ✅ Папка находится в `modules/`
- ✅ Основной файл имеет то же имя что и папка
- ✅ Файл содержит корректный PHP код

## Управление модулем

После создания модуль появится в админке Neetrino:
- В разделе **Neetrino > Dashboard** для включения/отключения
- В боковом меню админки (только когда активен)
- С переключателем ON/OFF на дашборде

### ❗ ВАЖНО: Статус модуля контролируется только с дашборда

**НЕ ДОБАВЛЯЙТЕ** в свой модуль кнопки или переключатели для включения/отключения модуля:
- ✅ Статус модуля управляется **только с дашборда Neetrino**
- ✅ Если модуль отключен на дашборде - он не должен работать
- ✅ Если модуль включен на дашборде - он должен полностью функционировать
- ❌ НЕ создавайте дублирующие элементы управления в интерфейсе модуля
- ❌ НЕ добавляйте собственные переключатели ON/OFF в настройках модуля

Система автоматически проверяет статус модуля через `Neetrino::is_module_active('module-name')`.

## Активация модулей

### ❌ ВАЖНО: Модули должны быть отключены по умолчанию

**ОБЯЗАТЕЛЬНОЕ ПРАВИЛО:** Все новые модули должны создаваться в отключенном состоянии. Пользователь должен сам принять решение о включении модуля.

### Исключения для активации по умолчанию

Только критически важные системные модули могут быть активны по умолчанию. Для добавления модуля в список активных по умолчанию требуется:

1. **Обоснование критической важности** модуля
2. **Согласование с командой разработки**
3. **Добавление в функцию** `get_default_active_modules()` в файле `neetrino.php`

```php
private static function get_default_active_modules() {
    // ВНИМАНИЕ: Добавляйте сюда только критически важные модули!
    // Все остальные модули должны активироваться пользователем вручную
    return ['bitrix24', 'login-page', 'maintenance-mode'];
}
```

### Текущие модули активные по умолчанию:
- `bitrix24` - интеграция с CRM системой
- `login-page` - кастомизация страницы входа
- `maintenance-mode` - режим технических работ

### Как пользователь активирует модуль

## Лучшие практики разработки модулей

### ✅ Рекомендуемая структура класса модуля:
```php
class Neetrino_My_Module {
    
    public function __construct() {
        // ОБЯЗАТЕЛЬНО: Проверка активности модуля
        if (!Neetrino::is_module_active('my-module')) {
            return;
        }
        
        // Инициализация только для активного модуля
        add_action('init', [$this, 'init']);
        add_action('wp_enqueue_scripts', [$this, 'enqueue_scripts']);
    }
    
    public function init() {
        // Основная логика модуля
        $this->load_settings();
        $this->setup_hooks();
    }
    
    private function load_settings() {
        // Загрузка настроек из базы данных
    }
    
    private function setup_hooks() {
        // Регистрация хуков и фильтров
    }
    
    /**
     * Страница настроек модуля (вызывается автоматически)
     */
    public static function admin_page() {
        // HTML интерфейс настроек
        // НЕ добавляйте здесь переключатели вкл/выкл модуля!
    }
}
```

### ✅ Управление настройками:
- Используйте `get_option()` и `update_option()` для сохранения настроек
- Префикс для настроек: `neetrino_{module_name}_settings`
- Не используйте глобальные переменные

### ❌ Что НЕ нужно делать в модуле:
- Не добавляйте переключатели "Включить/Отключить модуль"
- Не проверяйте статус модуля в методе `admin_page()`
- Не создавайте дублирующие элементы управления
- Не обходите систему проверки активности модуля

---

## Проверка работы

1. Создайте модуль по инструкции
2. Обновите страницу админки
3. Проверьте что модуль появился в **Neetrino > Dashboard**
4. Включите модуль переключателем
5. Проверьте что пункт меню появился в боковой панели
6. Убедитесь что страница настроек открывается

## Отладка

Если модуль не появился:
- Проверьте имена файлов и папок
- Убедитесь что нет PHP ошибок в коде
- Проверьте что файл находится в правильной директории
- Посмотрите логи PHP на наличие ошибок

---

## 📋 Справочник занятых цветов и иконок

### 🎨 Актуальные модули с Font Awesome иконками:

| Модуль | Slug | Цвет | Font Awesome Иконка | Описание |
|--------|------|------|---------------------|----------|
| Bitrix24 | `bitrix24` | 🟠 `#ff6600` | `fa-solid fa-network-wired` | Интеграция с CRM |
| Telegram | `telegram` | 🔵 `#0088cc` | `fa-brands fa-telegram` | Уведомления через Telegram |
| Maintenance Mode | `maintenance-mode` | 🟢 `#46b450` | `fa-solid fa-tools` | Режим тех. работ |
| Login Page | `login-page` | 🔵 `#3498db` | `fa-solid fa-lock` | Кастомизация входа |
| Auto Translate | `auto-translate` | � `#9b59b6` | `fa-solid fa-language` | Определение языка |
| Redirect 301 | `redirect-301` | � `#2563eb` | `fa-solid fa-arrows-turn-right` | Перенаправления |
| Menu Hierarchy | `menu-hierarchy` | 🔴 `#e74c3c` | `fa-solid fa-bars` | Настройка меню |
| Delete | `delete` | 🔴 `#dc3545` | `fa-solid fa-trash` | Массовое удаление |
| User Switching | `user-switching` | 🟡 `#f1c40f` | `fa-solid fa-users` | Переключение пользователей |
| WordPress Design | `wordpress-design` | 🔵 `#6c5ce7` | `fa-solid fa-palette` | Дизайн админ-панели |
| Reset | `reset` | � `#dc2626` | `fa-solid fa-rotate-left` | Сброс сайта |
| Remote Control | `remote-control` | 🟤 `#8b4513` | `fa-solid fa-satellite-dish` | Удаленное управление |
| Delivery | `delivery` | 🔵 `#1abc9c` | `fa-solid fa-truck` | Расчет доставки |
| Chat | `chat` | 🟢 `#2ecc71` | `fa-solid fa-comments` | Плавающие кнопки связи |

### 🎯 Свободные цвета для новых модулей:
- � **Светло-фиолетовый** `#9c88ff` - для интеграций
- � **Оранжевый** `#f39c12` - для утилит
- 🔵 **Голубой** `#17a2b8` - для аналитики

### 🎪 Рекомендуемые Font Awesome иконки по категориям:

**Безопасность:**
- `fa-solid fa-shield-halved` - защита
- `fa-solid fa-lock` - блокировка (занята)
- `fa-solid fa-user-shield` - приватность
- `fa-solid fa-key` - аутентификация

**Производительность:**
- `fa-solid fa-gauge-high` - производительность
- `fa-solid fa-chart-line` - мониторинг
- `fa-solid fa-chart-bar` - статистика
- `fa-solid fa-tachometer-alt` - скорость

**Контент:**
- `fa-solid fa-file-alt` - посты/документы
- `fa-solid fa-copy` - страницы
- `fa-solid fa-image` - медиа
- `fa-solid fa-edit` - редактирование

**Интеграции:**
- `fa-solid fa-plug` - плагины
- `fa-solid fa-network-wired` - сети (занята)
- `fa-solid fa-code` - API
- `fa-brands fa-telegram` - мессенджеры (занята)

**Утилиты:**
- `fa-solid fa-tools` - инструменты (занята)
- `fa-solid fa-cog` - настройки
- `fa-solid fa-puzzle-piece` - общие модули
- `fa-solid fa-magic` - автоматизация

**Популярные бренды:**
- `fa-brands fa-google` - Google интеграции
- `fa-brands fa-facebook` - Facebook
- `fa-brands fa-whatsapp` - WhatsApp
- `fa-brands fa-instagram` - Instagram

### ⚠️ При создании нового модуля:

1. **Проверьте таблицу** - не занят ли выбранный цвет
2. **Выберите подходящую иконку** из категории функционала
3. **Обновите эту таблицу** после создания модуля
4. **Протестируйте контрастность** цвета на белом фоне

---

**Версия системы:** Neetrino 2.0.0  
**Последнее обновление:** 30 июля 2025

---

## 🎉 Обновления v2.0.0

### ✨ Новая система иконок - Font Awesome!

**Что нового:**
- 🎨 **Font Awesome 6.4.0** - современные векторные иконки через CDN
- 🌈 **Автоматическое окрашивание** - иконки принимают цвет модуля
- ✨ **Плавные анимации** - hover эффекты и transitions
- � **Единая система** - `get_module_icon_html()` для всех модулей
- 🔄 **Обратная совместимость** - поддержка старых Dashicons

**Технические улучшения:**
- CDN загрузка Font Awesome для быстродействия
- Новое поле `icon_type` в конфигурации модулей
- CSS переменные для автоматического окрашивания
- Централизованная система управления иконками
- Поддержка брендовых иконок (`fa-brands`)

**Обновленные модули:**
- Все 14 модулей переведены на Font Awesome
- Telegram теперь использует официальную иконку бренда
- Улучшенная визуальная согласованность дашборда
